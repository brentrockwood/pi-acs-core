{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://acs.org/schema/v0.1.0/hooks/user-message.json",
  "title": "steps/userMessage payload",
  "description": "Payload for the userMessage hook. Fires when user input arrives, before LLM ingestion.",
  "type": "object",
  "required": ["content"],
  "properties": {
    "content": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["type", "value"],
        "properties": {
          "type": { "type": "string", "enum": ["text", "image", "audio", "file"] },
          "value": {},
          "provenance": {
            "$ref": "../provenance.json",
            "description": "OPTIONAL in the base schema; REQUIRED on every content item under provenance_producer: deterministic (ACS-Provenance). Validate against user-message.acs-provenance.json when the profile is claimed. See Specification section 7."
          }
        }
      }
    }
  }
}
